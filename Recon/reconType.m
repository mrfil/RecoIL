classdef reconType
    %reconType An enumeration class that defines the types of
    %reconstructions to run, rather than just using numbers
    %   Detailed explanation goes here
    enumeration
        COIL_IMAGES_ONLY, SOS_IMAGE_ONLY, SEN_MAP_ONLY, FM_MAP_ONLY, SENFM_MAPS
    end
    properties
    end
    
    methods
    end
    
end

